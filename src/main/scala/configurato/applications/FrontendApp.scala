package configurato.applications

import configurato.{Customisation, Environment}

object FrontendApp extends ConfigGenerator {
  override def generateConfig(e: Environment, c: Customisation): Unit = ???
}
